# Assets - Icons

Place custom SVG or PNG icon assets here.
Clean inline SVGs are utilized directly within the HTML markup for maximum rendering performance and sharp crispness across high-DPI displays.
